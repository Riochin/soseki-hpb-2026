# Supabase の接続文字列（ダッシュボード > Project Settings > Database > Connection string > URI）
DATABASE_URL=postgres://postgres:<password>@db.<project-ref>.supabase.co:5432/postgres

# フロントエンドのオリジン（開発時は localhost:3000）
ALLOWED_ORIGIN=http://localhost:3000

# 待ち受けポート（省略時は 8080）
PORT=8080
